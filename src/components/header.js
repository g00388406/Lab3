import React from "react";

export class Header extends React.Component{

    render(){
        return (
            <div>
                <h1>My header in another component</h1>
            </div>
        )
    }

    // ^ displaying the text to the browser

}